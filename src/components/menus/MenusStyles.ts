import styled from 'styled-components'
import Image from 'next/image'

export const StyleContainerMenu = styled.div `
  cursor: pointer;
  background-color: #FFFFFF;
  display: flex;
  gap: 20px;
`
export const StyledImageIcons = styled(Image)`
`